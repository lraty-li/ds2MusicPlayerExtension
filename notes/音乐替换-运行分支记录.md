# 音乐替换运行分支记录

## 目标

当前目标是为“音乐替换”继续收缩运行时边界。
下面只记录本轮已经得到的运行事实、明确反证，以及下一轮唯一优先确认点。

## 运行已命中

### 主音乐实例提交层

已命中 `sub_7FF69ED24EE0 -> sub_7FF69ED34D80` 这条路径，并且可以稳定绑定到：

```cpp
g_MusicRuntime->currentPlayerObj
```

这条路径当前只能作为观测点，作用是把：

```cpp
eventId
playingId
currentPlayerObj
GraphSoundPlayingSlot
```

关联起来。

它已经证明是主音乐实例的事件提交层，但还不是替换边界，因为这里拿不到：

```cpp
输出缓冲
写入长度
逻辑偏移/文件偏移
```

### AK::SoundEngine::PostEvent 提交后续

已命中：

```cpp
sub_7FF69EEDA220
sub_7FF69EF22F10
sub_7FF69EF22F50
```

这一组当前只证明了：

```cpp
eventId -> request -> playingId
```

的提交、分配和入队关系。

当前仍然只能归类为提交层/排队层，未出现字节流边界。

### 队列消费层

已命中：

```cpp
sub_7FF69EEF2AB0
```

这说明请求确实进入了消费层，而不是只停在排队层。

但当前已命中的消费支线是：

```cpp
sub_7FF69EEF2BCC
-> 0x7FF69EF75960
-> sub_7FF69EF62640
-> sub_7FF69EEFC9C0
```

其中：

```cpp
sub_7FF69EF62640
```

只是在全局表里按键查对象并加引用，返回命中项头部。

```cpp
sub_7FF69EEFC9C0
```

只改对象 `+0x18C` 的状态位。

这一支当前只能归类为事件/状态层，不是替换边界。

### 0x403/0x503 分支

已命中：

```cpp
0x7FF69EEF2BBF
```

但这次运行时真实目标对象不是：

```cpp
currentPlayerObj
```

该对象虚表 `+0x50` 的真实函数指针是：

```cpp
0x7FF69C7835E0
```

其行为仅为：

```cpp
*a2 = 0;
```

因此这个点当前只证明它是前置状态钩子，不是字节边界。

### 0x7FF69EFA29F0 支线

已命中：

```cpp
0x7FF69EFA29F0
-> sub_7FF69EF6C0C0
```

这次运行里：

```cpp
RCX != currentPlayerObj
RDX != currentPlayerObj
```

并且当前实际走的是“时长/计数累计”分支，而不是对象创建分支：

```cpp
*(DWORD *)(a2 + 0x84) += sub_7FF69EF58480(...)
```

这条已命中链依然没有出现：

```cpp
输出缓冲
写入长度
逻辑偏移/文件偏移
```

所以仍然不能作为替换边界。

## 新增反证

### 回调过滤反证

对：

```cpp
sub_7FF69ED33F10
```

加上“主音乐实例”条件后，在完整目标流程里零命中。

因此当前不能再把它视为主音乐实例的有效推进点。

### 退出态方法反证

`GraphSoundInstance::vftable + 0x118` 只在退出到标题界面时命中。

因此它只能归类为退出/状态切换点，不能当作常规播放主链。

### 队列候选反证

对：

```cpp
0x7FF69EEF2EB0
```

进行完整目标流程观测后零命中。

因此它应降级为反证，不再继续默认它是当前主消费边界。

## 当前结论

截至目前，已经把边界收缩到：

```cpp
主音乐实例事件提交层
-> 请求排队层
-> 请求消费层
-> 若干事件/状态对象处理层
```

但仍未命中真正满足替换实现条件的边界。

也就是：

```cpp
还没有任何点同时满足
1. 当前目标流程真实命中
2. 稳定关联当前目标资源
3. 拿到输出缓冲、写入长度、逻辑偏移/文件偏移
```

因此当前没有可直接落代码的音乐字节替换点。

## 下一轮唯一优先确认边界

下一轮唯一优先确认点是：

```cpp
0x7FF69EF6D970
```

原因不是静态命名，而是这次已命中对象的运行时虚表 `+616` 真实函数指针就是它。

它的当前逻辑是：

```cpp
if (sub_7FF69EF6D700() == 1)
    return sub_7FF69EF36550(a1, a1 + 224, a2);
```

如果完整目标流程里它再次零命中，就应把这条也记成新的明确反证。
