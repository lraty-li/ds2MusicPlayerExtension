# DS2 音乐流程实测结论：函数职责（UI 路径）

本文件只保留现有日志与 IDA MCP 能稳定支持的可靠结论，不记录计划、猜测、无法复现的单次现象或流水账。

## 已确认的函数职责

### `sub_141808720`

- 该次命中出现在点击播放样本里。
- 在一组较早样本里，它之后紧接着出现了 `sub_140C12920`。
- 在单曲活动列表 `trackId=57` 的点击播放样本里，它之后出现了：
  - `sub_140C14A00(index=0)`
  - `sub_140C12580`
- 这组单曲活动列表样本里，随后被直接播放的目标条目是：
  - `index=0`
  - `trackId=57`
- 当前样本里，它的 `manager=0x0`，前后 `managerState` 都是 `unreadable`。
- 在一组较新的“切到播放列表1并点击播放”样本里，当前 detour 记录到的 `manager.global` 槽位值同样为 `0x0`，但它后续仍然进入了带有效 `manager=0x301799B0000` 的真实播放链。
- 因此，当前 detour 里为 `sub_141808720` 读取的这组 `manager.global` 槽位，也还不能视为该运行路径里真实 manager 的可靠来源。
- 从 IDA 看，`sub_141808720` 开头会先读取 `qword_14619C988` 指向的另一组全局对象做一次查找，随后才单独读取 `qword_14619C428` 参与播放判定。
- 因此，`qword_14619C988` 和 `qword_14619C428` 不是同一个全局字段，不能继续把它们混成单一的“manager.global”语义。
- 从 IDA 看，它会先用 `sub_140C14BE0` 解析“当前播放曲目在可见列表里的条目”。
- 在运行时样本里，`sub_141808720` 后续至少已观察到三条不同分支：
  - 直接播放路径：`sub_140C12490`
  - 某类现有播放状态路径：`sub_140C12920`
  - 手动暂停样本里的现有对象状态切换路径：`sub_140C12780`
- 因此，当前不能再把它简化成“只会在 `sub_140C12490` 和 `sub_140C12920` 之间二选一”的结构。

### `sub_1416E34A0`

- `sub_1416E34A0` 直接使用 `qword_14619C428` 作为 manager，并位于一条更上层的 UI 分支中。
- 当 `manager+6416 == 1` 时，它会直接调用 `sub_140C12780`。
- 否则，它会先用 `sub_140C14BE0` 判断“当前播放曲目能否在可见列表里定位到条目”：
  - 若当前条目无效，或当前状态不满足另一分支条件，则调用 `sub_140C12490`
  - 否则调用 `sub_140C12920`
- 因此，它是当前已确认的一条“播放/暂停类按钮”的上层分流函数。

### `sub_1416E35C0`

- `sub_1416E35C0` 直接使用 `qword_14619C428` 作为 manager。
- 它会先用 `sub_140C14BE0` 判断当前播放曲目能否在可见列表里定位到条目，并要求 `manager+6472 > 1`。
- 条件满足时，它会调用 `sub_140C12F80`；当 `manager+6416 == 2` 时，随后还会再调用 `sub_140C12920`。
- 因此，它是当前已确认的一条“向前推进当前播放位置”的上层 UI 分支。

### `sub_1416E36E0`

- `sub_1416E36E0` 与 `sub_1416E35C0` 结构同构。
- 条件满足时，它会调用 `sub_140C132F0`；当 `manager+6416 == 2` 时，随后还会再调用 `sub_140C12920`。
- 因此，它是当前已确认的一条“向后回退当前播放位置”的上层 UI 分支。

### `sub_141809140`

- `sub_141809140` 也会先用 `sub_140C14BE0` 判断当前播放曲目能否在可见列表中定位到条目，并要求 `manager+6472 > 1`。
- 条件满足时，它会调用 `sub_140C12F80`；当 `manager+6416 == 2` 时，随后还会再调用 `sub_140C12920`。
- 因此，它与 `sub_1416E35C0` 一样，是 `sub_140C12F80` 的另一条上层 UI 路径。

### `sub_141809260`

- `sub_141809260` 与 `sub_141809140` 结构同构。
- 条件满足时，它会调用 `sub_140C132F0`；当 `manager+6416 == 2` 时，随后还会再调用 `sub_140C12920`。
- 因此，它与 `sub_1416E36E0` 一样，是 `sub_140C132F0` 的另一条上层 UI 路径。

### `sub_141809510`

- `sub_141809510` 会按 `manager+6444 / manager+6456 / manager+6464` 定位当前选中的可见条目。
- 当 `manager+6416 == 1` 时，它会先调用 `sub_140C12780`。
- 随后它会直接调用 `sub_140C15560(manager, selectedVisibleEntry)`。
- 因此，它位于“对当前选中的可见条目执行试听”的上层 UI 路径中。
- 运行时样本还进一步支持：试听目标跟随“当前选中的可见条目”，而不是跟随“当前正式播放曲目”。
- 在一组样本里，当前正式播放仍是 `trackId=5`，但重新进入播放器后光标落在第一首，随后试听命中的是：
  - `selectedIndex=0`
  - `entry=0x5044A028000`
  - `trackId=57`
- 在另一组样本里，当前正式播放同样仍是 `trackId=5`，但光标明确移动到第二首后，随后试听命中的是：
  - `selectedIndex=1`
  - `entry=0x5044A028038`
  - `trackId=5`
- 在一组真正重启进程后的新样本里，这一规律再次复现：
  - 当前正式播放是第一首 `trackId=57`
  - 光标选中第二首 `selectedIndex=1`
  - 随后试听命中 `entry=0x3145D818038 / trackId=5`
- 因此，当前运行时样本已经直接支持：`sub_141809510` 驱动的试听确实以“当前可见选中项”为目标。

### `sub_141809DA0`

- `sub_141809DA0` 会在一个上层 UI 条件满足时，直接重建当前来源列表和参与播放列表。
- 它会：
  - 清空当前 `sourceList6400` 对应的 100 项来源表区域
  - 遍历当前可见列表 `manager+6464`
  - 把所有 `entry+53 == 0` 且存在 `trackObject` 的可见条目 `trackId` 重新写入 `manager+6480 / manager+6472`
  - 最后调用 `sub_140C10E20(manager)`
- 因此，它更像“按当前可见列表整体重建来源表与参与播放列表”的上层同步入口。

### `sub_141808AE0`

- `sub_141808AE0` 直接接收当前选中的可见条目 `entry`。
- 当该条目的 `entry+48 >= 0` 时，它会按条目 `trackObject->trackId` 在 `manager+6480` 中找到并移除对应曲目。
- 移除后：
  - 若 `queueIndex6448 >= removedIndex` 且 `queueIndex6448 > 0`，它会把 `queueIndex6448` 减 1
  - 调用 `sub_140C10E20(manager)` 重建同步状态
  - 若当前正式播放曲目已无法再通过 `sub_140C14BE0` 定位到可见条目，则调用 `sub_140C12AC0(manager, -1, ...)`
- 在一组“列表3里只有 `trackId=57`，当前未播放”的受控样本里，还观察到：
  - 目标条目进入 `sub_141808AE0` 前是 `state30=0 / entryState48=0`
  - 到 `sub_140C10E20.before` 时，已经是 `trackIdListCount6472=0 / trackIdList6480=null`
  - `sub_140C10E20.after` 后同一条目变成 `state30=-1 / entryState48=-1`
  - 这次没有进入 `sub_140C12AC0`
- 在另一组“列表3里只有 `trackId=57`，且它正处于正式播放中”的受控样本里，还观察到：
  - 到 `sub_140C10E20.before` 时，已经是 `trackIdListCount6472=0 / currentTrackId6436=57`
  - `sub_140C10E20.after` 后，当前条目与当前可见条目都变成 `state30=-1 / entryState48=-1`
  - 随后立即进入 `sub_140C12AC0(manager, -1, ...)`
- 因此，它是当前已确认的“从活动列表移除当前选中曲目”的上层 UI 路径。

### `sub_1418098A0`

- `sub_1418098A0` 会先调用 `sub_140C14BE0` 判断“当前播放曲目”能否在当前可见列表中定位到条目。
- 如果能定位到，它会直接把该条目的 `itemId44` 写回 UI 选中项。
- 如果当前条目无效，它会在可见列表里查找一个：
  - `entry+53 == 0`
  - 且 `trackObject->trackId == manager+6480[0]`
  的条目，再把该条目的 `itemId44` 写回 UI。
- 因此，它更像“把当前播放状态回写到 UI 选中项”的收束点。

### `sub_141808C00`

- 在当前样本里，它已出现在：
  - 进入播放器页面
  - 空闲状态下光标移动
- 在这些样本里，它都出现在 `sub_140C155F0` 之前，并且随后会再次触发 `sub_140C14A00` 的整表枚举。
- 在单曲活动列表样本里，已观察到：
  - `uiSelectedIndex=1` 时，`returnValue=0x55D83018000`
  - `uiSelectedIndex=0` 时，`returnValue=0x55D83018000`
  - 随后的 `sub_140C14A00(index=0)` 返回 `entry=0x55D83018000`
- 在另一组页面进入样本里，还观察到：
  - `uiSelectedIndex=0`
  - `returnValue=0x0`
  - 随后的 `sub_140C14A00(index=0)` 返回 `entry=0x4E4FD428038, trackId=5`
- 在另一组单曲活动列表光标移动样本里，还观察到：
  - `uiSelectedIndex=1` 时，`returnValue=0x4E4FD428000`
  - `uiSelectedIndex=0` 时，`returnValue=0x4E4FD428000`
  - 同期 `sub_140C14910` 可分别解析到 `0x4E4FD428000` 与 `0x4E4FD428038`
  - 而后续 `sub_140C14A00(index=0)` 仍返回 `0x4E4FD428038`
- 在另一组较新的单曲活动列表光标移动样本里，还观察到：
  - `uiSelectedIndex=1` 时，`returnValue=0x29332438000`
  - `uiSelectedIndex=0` 时，`returnValue=0x29332438000`
  - 同期 `sub_140C14910` 可分别解析到 `0x29332438000` 与 `0x29332438038`
- 因此，当前运行时样本已经证明：`sub_141808C00` 的 `returnValue` 不能稳定视为“当前选中索引”，也不能稳定视为“最终活动列表条目指针”。
- 相比之下，当前样本里 `sub_140C14910` 更能反映“这一步解析到的是哪个相邻槽位”。
- 当前样本也说明，`uiSelectedIndex` 与最终落到的活动列表条目位置不一定相同。
- 当前样本里，它记录到的 `manager=0x0`，并且 `playlist.before / playlist.after` 相关字段都为 `unreadable`。
- 因此，当前 detour 中 `sub_141808C00` 的 `manager` 与 `playlist.before / playlist.after` 结果也不能视为可靠结论。
- 基于当前运行时样本，只能确认 `sub_141808C00` 与页面进入后的当前项处理、以及光标移动后的当前项变化处理相关。
