# DS2 音乐流程实测结论：函数职责（列表核心）

本文件只保留现有日志与 IDA MCP 能稳定支持的可靠结论，不记录计划、猜测、无法复现的单次现象或流水账。

## 已确认的函数职责

### `sub_140C10C20`

- `sub_140C10C20` 会按 `manager+6472` 重建 `manager+6496` 上的一组顺序数组。
- 它会先把这组数组填成 `0..count-1`，再做一次洗牌。
- 洗牌后，它还会把当前 `manager+6448` 对应的位置换到队列头部。
- `sub_140C10C20` 的 `returnValue` 与 `manager+6496` 的顺序数组指针一致，不是列表条目指针。
- 在单曲受控样本里，已观察到：
  - `trackIdListCount6472=1`
  - `trackIdList6480=[5]`
  - `queueOrder6496.after=[0]`
  这说明当参与队列构建的曲目列表只有 1 首时，`6496` 的结果就是唯一索引 `0`。
- 在同一组单曲受控样本里，已观察到：
  - `visibleCount6456=30`
  - `trackIdListCount6472=1`
  因此，`manager+6456` 不是 `6480/6496` 这组播放顺序列表的数量。
- 当前已观察到的 `sub_140C10C20` 调用路径至少包括：
  - `DS2+0xC10BD1`：切换播放列表
  - `DS2+0xC12681`：点击播放前的播放顺序预处理
- 在一组“把第四首歌加入播放列表1”的受控样本里，还观察到：
  - `trackIdList6480.before=[5,20]`
  - `queueOrder6496.after=[0,1]`
  - 随后的 `sub_140C14A00(index=0/1)` 返回 `[5,20]`
- 这说明在这次样本里，`sub_140C10C20` 处理的不是单曲列表，而是已经扩展成两首歌的参与播放列表。
- 因此，它更像“基于当前活动列表位置生成播放队列顺序”的预处理点，而不是直接播放入口。

### `sub_140C109E0`

- `sub_140C109E0` 会按固定顺序执行：
  - `sub_140C10AA0(manager, manager+6400, manager+6472)`
  - `sub_140C10B90(manager, 0)`
  - `sub_140C10C20(manager)`
- 在上述三步之后，它还会遍历当前可见列表 `manager+6464`，范围由 `manager+6456` 给出。
- 遍历可见列表时，它会先把每个条目的 `entry+48` 设为 `-1`。
- 对于 `entry+53 == 0` 且存在 `trackObject` 的可见条目，它会按 `trackObject->trackId` 在 `manager+6480` 中查找位置，并把找到的 0-based 位置写回该条目的 `entry+48`。
- 在一组“切到播放列表1并点击播放”的受控样本里，进入 `sub_140C109E0` 前观察到：
  - `sourceList6400=0`
  - `trackIdListCount6472=0`
  - `queueOrder6496 count=0`
  返回后观察到：
  - `trackIdListCount6472=1`
  - `trackIdList6480=[5]`
  - `queueOrder6496=[0]`
- 这说明在该受控样本里，`sub_140C109E0` 运行时确实完成了“先生成参与播放的 `6480` trackId 列表，再重建 `6496` 顺序数组”。
- 在同一组受控样本里，`sub_140C109E0 returnValue=trackIdList6480.ptr + 4`，因此它的返回值更像 `6480` 缓冲区的写入结束位置，而不是列表条目指针，也不是 `6496` 指针。
- 在同一组受控样本里，`sub_140C109E0 before/after visible0` 都是 `trackId=57` 的可见槽位，且 `entryState48=-1`；但紧随其后的 `sub_140C14A00(index=0)` 返回的是 `trackId=5`。
- 因此，至少在当前样本里，`sub_140C109E0` 记录到的 `visible0` 不是 `sub_140C14A00(index=0)` 所读取的“当前活动列表第 0 项”。
- 在一组“把第四首歌加入播放列表1”的受控样本里，也同样观察到：
  - `sub_140C109E0 before/after visible0=trackId=57`
  - 但后续活动列表 `sub_140C14A00(index=0/1)` 返回的是 `[5,20]`
- 这进一步支持：`manager+6464` 上的可见槽位，与 `sub_140C14A00` 读取的当前活动列表不是同一层结构。
- 因此，`sub_140C109E0` 更像“按当前来源列表重建参与播放的 trackId 列表、同步 queueIndex、重建队列顺序，并把可见条目映射回该队列位置”的核心重建点。

### `sub_140C10AA0`

- `sub_140C10AA0` 会把输入的来源列表编号收束到 `0..9`。
- 它会扫描 `manager + 400 * (sourceList6400 + 6)` 这一组 100 项的 trackId 表。
- 只有满足以下条件的 trackId，才会被追加到 `manager+6480 / manager+6472`：
  - trackId 非 0
  - 该 trackId 能在当前可见列表 `manager+6464` 中找到
  - 对应可见条目 `entry+53 == 0`
- 因此，`manager+6480` 不是当前可见列表的直接拷贝，而是“当前来源列表”和“当前可见列表中可用条目”的交集结果。
- 在一组“切到播放列表1”的受控样本里，运行时还观察到：
  - `sourceList=0`
  - `sourceTable.before ... nonZeroCount=1`
  - `firstValues=0x00000005`
  - `outList.after=[5]`
- 这说明在该受控样本里，`sourceList=0` 对应的来源表实际筛进 `6480` 的曲目就是 `trackId=5`。

### `sub_140C10B90`

- `sub_140C10B90(manager, requestedIndex)` 会先把 `manager+6448` 暂写为 `requestedIndex`。
- 如果当前 `manager+6436` 上的 `trackId` 不为 0，它会在 `manager+6480` 中查找这个当前曲目的位置，并改写 `manager+6448`。
- 如果当前曲目不在 `manager+6480` 中，它会把 `manager+6448` 收束回有效范围；当 `manager+6472 > 0` 时，最终结果至少会落到 `0`。
- 因此，`sub_140C10B90` 更像“按当前曲目在参与播放列表中的位置重建 queueIndex6448”的同步点。
- 在一组“切到播放列表1”的受控样本里，还观察到：
  - `requestedIndex=0`
  - `currentTrackId6436=0`
  - `trackIdList6480=[5]`
  - 前后 `queueIndex6448=0`
- 这说明在“当前尚无正式播放曲目”的单曲样本里，`sub_140C10B90(manager, 0)` 会把 `queueIndex6448` 收束在 `0`。

### `sub_140C10D90`

- 在一组“把第四首歌加入播放列表1”的受控样本里，已观察到调用链：
  - `sub_141808320`
  - `sub_140C14910`
  - `sub_140C149C0`
  - `sub_141808A80`
  - `sub_140C10D90`
  - `sub_140C10C20`
- 这组样本里，`sub_140C10D90` 的目标条目是：
  - `entry=0x51034C380A8`
  - `trackId=20`
  - `itemId2C=3`
- 到这次样本里的 `sub_140C10C20.before` 时，已经观察到：
  - `trackIdList6480=[5,20]`
  - `trackIdListCount6472=2`
- 因此，在这次“加入播放列表”样本里，`sub_140C10D90` 位于 `6480` 已被改写、`6496` 尚未重建的关键位置。
- 在另一组“空的播放列表3中加入第一首歌 `trackId=57`”的受控样本里，还观察到：
  - `sub_141808A80 -> sub_140C10D90 -> sub_140C10E20 -> sub_140C10C20`
  - 到 `sub_140C10E20.before` 时，已经是 `trackIdList6480=[57]`
  - 同期该条目仍是 `state30=-1 / entryState48=-1`
- 因此，当前样本进一步支持：`sub_140C10D90` 更像“把目标曲目写入活动播放列表数据”的关键点，而 `sub_140C10E20` 更像后续同步收束点。

### `sub_140C10E20`

- `sub_140C10E20` 会在活动播放列表发生变更后，再调用一次 `sub_140C10C20` 重建 `6496` 队列顺序。
- 在一组“空的播放列表3中加入第一首歌 `trackId=57`”的受控样本里，已观察到：
  - `sub_140C10E20.before` 时已经是 `trackIdList6480=[57]`
  - 同一个可见条目在 `before -> after` 之间从 `entryState48=-1 / state30=-1` 变为 `entryState48=0 / state30=0`
  - 随后的 `sub_140C14A00(index=0)` 返回的也正是该条目 `trackId=57`
- 在一组“把列表3里唯一的 `trackId=57` 移除，且当前未播放”的受控样本里，已观察到：
  - `sub_140C10E20.before` 时已经是 `trackIdListCount6472=0 / trackIdList6480=null`
  - 同一个可见条目在 `before -> after` 之间从 `entryState48=0 / state30=0` 变为 `entryState48=-1 / state30=-1`
- 在一组“先播放 `trackId=57`，再把它从列表3移除”的受控样本里，还观察到：
  - `sub_140C10E20.before` 时已经是 `currentTrackId6436=57`，但 `trackIdListCount6472=0`
  - 同一个当前条目在 `before -> after` 之间从 `entryState48=0 / state30=0` 变为 `entryState48=-1 / state30=-1`
  - 紧接着进入 `sub_140C12AC0`
- 因此，当前运行时样本强烈支持：
  - `sub_140C10E20` 不是把 trackId 首次写入 `6480` 的起点
  - 它更像“活动播放列表变化后，把可见条目状态同步到新结果，并触发队列顺序重建”的关键收束点

### `sub_141808A80`

- 在一组“把第四首歌加入播放列表1”的受控样本里，`sub_141808A80` 直接拿到的 `arg1` 就是目标条目：
  - `entry=0x51034C380A8`
  - `trackId=20`
- 同一组样本里，它随后进入了 `sub_140C10D90`，并且 `returnValue=1`。
- 在另一组“空的播放列表3中加入第一首歌 `trackId=57`”的受控样本里，它同样进入了 `sub_140C10D90`，且曲目已被成功加入；但这次 `returnValue=0`。
- 因此，当前不能把 `sub_141808A80 returnValue` 直接当作通用的“加歌成功/失败”布尔值。
- 因此，它位于“把当前选中曲目加入播放列表”的上层 UI 路径中。

### `sub_141808320`

- 在一组“把第四首歌加入播放列表1”的受控样本里，`sub_141808320` 位于整条加歌路径的最上层：
  - `sub_141808320`
  - `sub_140C14910`
  - `sub_140C149C0`
  - `sub_141808A80`
  - `sub_140C10D90`
  - `sub_140C10C20`
- 在另一组“从列表3移除当前选中曲目”的受控样本里，它位于整条移除路径的最上层：
  - `sub_141808320`
  - `sub_140C14910`
  - `sub_140C149C0`
  - `sub_141808AE0`
  - `sub_140C10E20`
  - `sub_140C10C20`
- 因此，它与“对当前选中曲目执行加入/移除操作”的 UI 入口直接相关。

### `sub_140C13660`

- `sub_140C13660(manager, newState)` 会更新 `manager+6416` 上的状态字节。
- 当目标状态为 `0` 时，它会遍历 `manager+10392` 开始的一组 56 字节槽位，槽位数量由 `manager+10404` 给出。
- 在这条 `newState=0` 路径里，它会清理这些槽位里偏移 `+24` 和 `+32` 上持有的 runtimeObject，并调用 `sub_140AC5320` 销毁它们。
- 因此，退出时看到的那批 `music-flow.other` runtimeObject 销毁，与 `sub_140C13660` 这条状态收束路径直接一致。

### `sub_140C149C0`

- `sub_140C149C0` 会读取 `manager+6444`，按 `manager+6456` 做边界收束，再从 `manager+6464` 返回当前选中位置对应的可见列表条目。
- 当 `manager+6444 < 0` 时，它会回落到当前可见列表的第 0 项。

### `sub_140C14910`

- `sub_140C14910` 也会按 `manager+6444` 和 `manager+6456` 收束当前选中位置，再定位到 `manager+6464` 中的当前选中条目。
- 当该条目的 `entry+52 != 0` 时，`sub_140C14910` 会把这个字节清零，并进一步更新该条目关联对象上的状态位。
- 在一组单曲活动列表受控样本里，它曾解析到相邻两个 56 字节槽位：
  - `0x4E4FD428000 -> trackId=57`
  - `0x4E4FD428038 -> trackId=5`
- 在另一组单曲活动列表光标移动样本里，它也曾解析到相邻两个 56 字节槽位：
  - `0x29332438000 -> trackId=57`
  - `0x29332438038 -> trackId=5`
- 在这组较新的光标移动样本里，上述两个相邻槽位的 `state30` 都是 `0xFFFFFFFF`。
- 因此，至少在当前样本范围内，不能再把 `state30` 单独当作区分这两个相邻槽位语义的依据。
- 同一个 `0x4E4FD428000` 地址后续也曾通过 `sub_140C14A00(index=0)` 作为正常活动列表条目返回，因此不能仅凭某一次 `state30=0xFFFFFFFF` 就把它视为固定无效槽位。

### `sub_140C12490`

- `sub_140C12490` 是一条基于 `queueIndex` 的播放路径。
- 从 IDA 看，`sub_140C12490` 体内是直接读取 `qword_14619C428` 作为 manager，而不是使用它的传入参数来定位 manager。
- 它在末尾会执行：
  - `sub_140C14A00(manager, manager+6448)`
  - `sub_140C12580(manager, entry)`
- 因此，它会按当前 `queueIndex` 取出活动列表条目，再直接进入正式播放入口。
- 在单曲活动列表 `trackId=5` 的点击播放样本里，运行时还观察到：
  - `sub_140C10C20`
  - `sub_140C14A00(index=0)`
  - `sub_140C12580(entry=0x29332438038, trackId=5)`
  这说明它在正式取项并进入播放前，还会先走一次播放顺序预处理。
- 在一组单曲活动列表 `trackId=5` 的点击播放样本里，它后续实际导向了：
  - `sub_140C14A00(index=0)`
  - `sub_140C12580(entry=0x4E4FD428038, trackId=5)`
- 这说明它在运行时确实能把当前活动列表条目送入正式播放入口。
- 在一组较新的“切到播放列表1并点击播放”样本里，当前 detour 记录到的 `manager.global` 槽位值为 `0x0`，但后续同一次点击路径里：
  - `sub_140C10C20`
  - `sub_140C14A00`
  - `sub_140C12580`
  都实际使用了有效 `manager=0x301799B0000`。
- 因此，当前 detour 里为 `sub_140C12490` 读取的这组 `manager.global` 槽位，还不能视为该运行路径里真实 manager 的可靠来源。

### `sub_140C14BE0`

- `sub_140C14BE0` 会在当前可见列表 `manager+6464` 中，按 `manager+6436` 的当前 `trackId` 查找对应条目。
- 它查找时会跳过 `entry+53 != 0` 的条目。
- 找到后返回对应的可见列表条目；找不到时返回 `manager+10208` 上的一个哨兵槽位。
- 因此，它更像“当前播放曲目在当前可见列表中的定位函数”。
