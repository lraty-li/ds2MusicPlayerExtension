# DS2 音乐流程旧方案记录：外部 WEM 注入原型（已证伪）

本页记录一条已经做过运行时验证、但已被证伪的旧原型。

它不再代表当前主线方案，只保留给后续回顾使用，避免重复走回头路。

## 旧原型当时的假设

- 高层先在 `sub_140C12580 / sub_140C15560` 拿到目标 `trackId`。
- 再在 `sub_1426E7CC0` 拿到即将交给播放链的输出缓冲。
- 如果这两步都成立，就可以把外部 `.wem` 的字节覆写到该缓冲区里。

## 旧原型的实现思路

- `sub_140C12580`
  - 正式播放入口。
  - 负责按 `trackId` 选择外部文件。
- `sub_140C15560`
  - 试听入口。
  - 同样负责按 `trackId` 选择外部文件。
- `sub_1426E7CC0`
  - 被当作底层注入点候选。
  - 旧原型希望在这里把外部 `.wem` 的字节覆写进返回缓冲。

当时外部文件目录固定为：

`scripts\external_music\`

查找顺序为：

- 正式播放优先：
  - `play_<trackId>.wem`
  - `<trackId>.wem`
  - `play.wem`
  - `override.wem`
- 试听优先：
  - `preview_<trackId>.wem`
  - `<trackId>.wem`
  - `preview.wem`
  - `override.wem`

## 已拿到的运行时结果

- 已看到 `externalMusic armed`，说明：
  - 高层播放/试听入口已经能拿到目标 `trackId`
  - 外部文件选择层本身可以工作
- 但同一轮运行日志里没有看到：
  - `hit sub_1426E7CC0`
  - `externalMusic override hit`
- 同时，实机听到的仍然是原版音乐，而不是目录里的外部 `.wem`。

## 当前结论

- 这条旧原型只证明了：
  - 我们可以在高层选曲入口稳定挑中一个外部文件
- 这条旧原型没有证明：
  - 外部字节已经真正进入播放器链
- 因而可以明确写成结论：
  - `sub_1426E7CC0` 不是当前可接受的外部 WEM 注入点候选
  - 或者至少当前“在 `sub_1426E7CC0` 覆写输出缓冲”的假设没有命中真实音乐数据路径

## 还能复用的部分

- 外部目录约定 `scripts\external_music\` 仍然可以保留。
- 按 `trackId` 区分 `play_<trackId>.wem / preview_<trackId>.wem` 的命名方式仍然有价值。
- `sub_140C12580 / sub_140C15560` 作为高层“选中哪首歌”的观察入口，仍然是可信的。

## 不应再继续沿用的结论

- 不应再把 `sub_1426E7CC0` 写成“当前原型的底层注入点”。
- 不应再把“覆写 `sub_1426E7CC0` 输出缓冲”视为当前主线实现方案。
